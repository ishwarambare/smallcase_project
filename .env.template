# ============================================================
# Environment Configuration Template
# ============================================================
# This file is a template for environment variables. 
# For LOCAL development, copy this to .env and fill in values.
# For RAILWAY deployment, set these in Railway Dashboard.
# ============================================================

# ============ Django Core Settings ============
# Generate a new SECRET_KEY for production using:
#   python generate_secret_key.py
SECRET_KEY=your_secret_key_here

# DEBUG mode
# LOCAL: DEBUG=True
# RAILWAY PRODUCTION: DEBUG=False (CRITICAL for security!)
DEBUG=False

# Allowed Hosts
# LOCAL: ALLOWED_HOSTS=localhost,127.0.0.1
# RAILWAY: ALLOWED_HOSTS=.railway.app,.up.railway.app,yourdomain.com
ALLOWED_HOSTS=.railway.app,.up.railway.app,localhost,127.0.0.1

# CSRF Trusted Origins
# LOCAL: CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
# RAILWAY: CSRF_TRUSTED_ORIGINS=https://*.railway.app,https://*.up.railway.app
CSRF_TRUSTED_ORIGINS=https://*.railway.app,https://*.up.railway.app

# ============ Database Configuration ============
# By default, uses SQLite for local development (no config needed)
# To use PostgreSQL, uncomment and set DATABASE_URL:
#
# Format: postgresql://username:password@host:port/database_name
#
# LOCAL PostgreSQL examples:
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/smallcase_db
# DATABASE_URL=postgresql://smallcase_user:secure_pass@localhost:5432/smallcase_db
#
# DOCKER PostgreSQL:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smallcase_db
#
# RAILWAY: Railway auto-provides DATABASE_URL when PostgreSQL addon is added
# No configuration needed - it's handled automatically!

# ============ AI Chat Configuration ============
# Choose provider: 'groq' (default, free 30 RPM) or 'gemini' (free 15 RPM)
AI_PROVIDER=groq

# Groq API Configuration
# Get API key from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Google Gemini API Configuration
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# Hugging Face API Configuration (Used for RAG model and embeddings)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# ============ Email Configuration ============
# For development, use console backend (prints to terminal)
# For production, use SMTP backend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# --- Gmail SMTP Configuration ---
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. For production, set EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your.email@gmail.com
EMAIL_HOST_PASSWORD=your_gmail_app_password_here
DEFAULT_FROM_EMAIL=your.email@gmail.com

# --- Alternative: SendGrid SMTP (100 emails/day free) ---
# Signup: https://sendgrid.com
# Uncomment these if using SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your_sendgrid_api_key_here

# ============ SMS Configuration (Twilio) ============
# Signup at https://www.twilio.com (free $15 credit trial)
# Get credentials from: https://console.twilio.com
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890

# Alternative: MSG91 (Good for India)
# MSG91_AUTH_KEY=your_msg91_auth_key_here
# MSG91_SENDER_ID=your_sender_id_here

# ============ Social Authentication (OAuth) ============

# --- Google OAuth ---
# 1. Go to: https://console.cloud.google.com/
# 2. Create a project and enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs:
#    LOCAL: http://localhost:8000/accounts/google/login/callback/
#    PRODUCTION: https://yourdomain.com/accounts/google/login/callback/
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret_here

# --- GitHub OAuth ---
# 1. Go to: https://github.com/settings/developers
# 2. Create a new OAuth App
# 3. Set Authorization callback URL:
#    LOCAL: http://localhost:8000/accounts/github/login/callback/
#    PRODUCTION: https://yourdomain.com/accounts/github/login/callback/
GITHUB_OAUTH_CLIENT_ID=your_github_client_id_here
GITHUB_OAUTH_CLIENT_SECRET=your_github_client_secret_here

# ============ Backblaze B2 Storage (Optional) ============
# Only needed if using B2 for static file storage
BACKBLAZE_APPLICATION_KEY_ID=your_key_id_here
BACKBLAZE_APPLICATION_KEY=your_application_key_here
BACKBLAZE_BUCKET_NAME=your_bucket_name_here
BACKBLAZE_S3_ENDPOINT_URL=https://s3.us-east-005.backblazeb2.com

# ============================================================
# RAILWAY DEPLOYMENT CHECKLIST:
# ============================================================
# 1. Generate production SECRET_KEY: python generate_secret_key.py
# 2. Set DEBUG=False
# 3. Configure ALLOWED_HOSTS with your Railway domain
# 4. Set CSRF_TRUSTED_ORIGINS with https URLs
# 5. Add AI API keys (GROQ_API_KEY or GEMINI_API_KEY)
# 6. Configure email settings for production
# 7. Add OAuth credentials (Google, GitHub)
# 8. Add Twilio credentials if using SMS
# 9. Railway auto-provides DATABASE_URL for PostgreSQL
# 10. Run migrations: python manage.py migrate
# 11. Collect static files: python manage.py collectstatic --noinput
# ============================================================

# Railway Environment
RAILWAY_ENVIRONMENT=True

# ============ Supabase S3 Storage (Optional) ============
# To enable Supabase/S3 file storage, set these variables:
# AWS_ACCESS_KEY_ID=14cad28b0b0aff12a80441f500afc299
# AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
# AWS_STORAGE_BUCKET_NAME=smallcase
# AWS_S3_ENDPOINT_URL=https://yvcrrebrvlnafvdcdiwh.storage.supabase.co/storage/v1/s3
# AWS_S3_REGION_NAME=ap-southeast-2


